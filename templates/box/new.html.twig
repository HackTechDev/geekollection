{% extends 'base.html.twig' %}
  
{% block title %}New Box{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create Box</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_box_index') }}"> 
                    View All Boxs
                </a>
            </div>
            <div class="card-body">
                 {{ include('box/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
