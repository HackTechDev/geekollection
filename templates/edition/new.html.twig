{% extends 'base.html.twig' %}
  
{% block title %}New Edition{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create Edition</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_edition_index') }}"> 
                    View All Editions
                </a>
            </div>
            <div class="card-body">
                 {{ include('edition/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
