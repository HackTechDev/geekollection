{% extends 'base.html.twig' %}
  
{% block title %}Objectlink index{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Objectlink Manager</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-primary" href="{{ path('app_objectlink_new') }}"> 
                    Create New Objectlink 
                </a>
            </div>
            <div class="card-body">
                <table class="table table-bordered">
                    <tr>
                        <th>Link</th>
                        <th>Date Created</th>
                        <th>Date Updated</th>
                        <th width="240px">Action</th>
                    </tr>
                    {% for objectlink in objectlinks %}
                    <tr>
                        <td>{{ objectlink.link }}</td>
                        <td>{{ objectlink.createdAt ? objectlink.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                        <td>{{ objectlink.updatedAt ? objectlink.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                        <td>
                            <form method="post" action="{{ path('app_objectlink_delete', {'id': objectlink.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                                <a
                                    class="btn btn-outline-info"
                                    href="{{ path('app_objectlink_show', {'id': objectlink.id}) }}">
                                    Show
                                </a>
                                <a
                                    class="btn btn-outline-success"
                                    href="{{ path('app_objectlink_edit', {'id': objectlink.id}) }}">
                                    Edit
                                </a>
  
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objectlink.id) }}">
                                <button class="btn btn-outline-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                    {% endfor %}
                </table>
            </div>
        </div>
    </div>
  
{% endblock %}
