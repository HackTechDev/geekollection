{% extends 'base.html.twig' %}
  
{% block title %}New Project{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create Library</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_library_index') }}"> 
                    View All Libraries
                </a>
            </div>
            <div class="card-body">
                 {{ include('library/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}