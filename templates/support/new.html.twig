{% extends 'base.html.twig' %}
  
{% block title %}New Support{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create Support</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_support_index') }}"> 
                    View All Supports
                </a>
            </div>
            <div class="card-body">
                 {{ include('support/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
