{% extends 'base.html.twig' %}
  
{% block title %}Edit Support{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Support</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_support_index') }}"> 
                    View All Supports
                </a>
            </div>
            <div class="card-body">
                {% include 'support/_form.html.twig' with {'button_label': 'Update Support', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}
